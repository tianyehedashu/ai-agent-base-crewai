# 跨境电商智能运营系统 - 混合架构方案

**Web 端集中管理 + 客户端本地能力**

## 1. 架构愿景

### 1.1 核心需求

你的需求非常清晰，需要一个**混合架构**系统：

```
┌─────────────────────────────────────────────────────────────┐
│                    核心需求                                  │
│                                                              │
│  ✅ 集中管理与发布                                           │
│     • Crew/Flow 配置管理                                     │
│     • Agent 模板库                                           │
│     • 运营策略共享                                           │
│     • 团队协作                                               │
│                                                              │
│  ✅ 本地工具集成能力                                         │
│     • MCP (Model Context Protocol) 支持                     │
│     • 本地数据访问                                           │
│     • 本地 API 调用                                          │
│     • 文件系统操作                                           │
│                                                              │
│  ✅ 类似 Claude Code 但面向运营                              │
│     • 对话式交互                                             │
│     • 上下文理解                                             │
│     • 智能建议                                               │
│     • 但专注于电商运营场景                                   │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 架构对比

| 特性 | 纯 Web 端 | 纯客户端 | **混合架构** ✅ |
|------|----------|---------|----------------|
| 集中管理 | ✅ 优秀 | ❌ 困难 | ✅ 优秀 |
| 本地工具 | ❌ 受限 | ✅ 完整 | ✅ 完整 |
| 团队协作 | ✅ 优秀 | ❌ 困难 | ✅ 优秀 |
| 部署难度 | ✅ 简单 | ⚠️ 中等 | ⚠️ 中等 |
| 数据安全 | ⚠️ 云端 | ✅ 本地 | ✅ 可选 |
| 离线能力 | ❌ 无 | ✅ 完整 | ⚠️ 部分 |

**结论**：混合架构是最佳选择！

## 2. 整体架构设计

### 2.1 三层架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Web 管理端 (云端)                         │
│  • Crew/Flow 配置管理                                        │
│  • Agent 模板库                                              │
│  • 策略共享与版本控制                                        │
│  • 团队协作与权限管理                                        │
│  • 执行历史与分析                                            │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTPS/WebSocket
                           │ 同步配置、共享策略
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                  桌面客户端 (本地)                           │
│  • 对话式交互界面                                            │
│  • 本地 Crew 执行引擎                                        │
│  • MCP 服务集成                                              │
│  • 本地工具调用                                              │
│  • 离线模式支持                                              │
└──────────────────────────┬──────────────────────────────────┘
                           │ MCP Protocol
                           │ 本地工具调用
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                  MCP 工具层 (本地)                           │
│  • 文件系统访问                                              │
│  • 数据库连接                                                │
│  • 内部 API 调用                                             │
│  • 本地脚本执行                                              │
│  • Excel/CSV 处理                                            │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 数据流

```
用户在客户端发起请求
    ↓
客户端检查：需要云端资源？
    ↓
┌─────────────────┐
│ 需要云端？       │
└────┬────────┬───┘
     │ 是     │ 否
     ↓        ↓
从 Web 端     完全本地执行
获取配置      (使用本地 Crew)
     ↓        ↓
在本地执行    调用 MCP 工具
(Crew AI)         ↓
     ↓        访问本地资源
调用 MCP 工具     ↓
     ↓        返回结果
返回结果          ↓
     ↓        显示给用户
上传执行日志
到 Web 端
```

## 3. Web 管理端设计

### 3.1 核心功能

```
┌─────────────────────────────────────────────────────────────┐
│                    Web 管理端                                │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  1. Crew 配置管理                                   │    │
│  │  • 可视化 Crew 编辑器                              │    │
│  │  • Agent 配置                                       │    │
│  │  • Task 定义                                        │    │
│  │  • Flow 编排                                        │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  2. 模板库                                          │    │
│  │  • Agent 模板（数据分析师、策略师等）              │    │
│  │  • Task 模板（数据收集、策略生成等）               │    │
│  │  • Crew 模板（完整的运营流程）                     │    │
│  │  • 模板市场（团队共享）                            │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  3. 策略共享                                        │    │
│  │  • 策略发布与订阅                                   │    │
│  │  • 版本控制                                         │    │
│  │  • 权限管理                                         │    │
│  │  • 评论与反馈                                       │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  4. 执行监控                                        │    │
│  │  • 实时执行状态                                     │    │
│  │  • 历史记录查询                                     │    │
│  │  • 性能分析                                         │    │
│  │  • 成本统计                                         │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  5. 团队协作                                        │    │
│  │  • 多用户管理                                       │    │
│  │  • 角色权限                                         │    │
│  │  • 审批流程                                         │    │
│  │  • 活动日志                                         │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 技术栈

```python
# Web 管理端技术栈
{
    "frontend": {
        "framework": "React 18 + TypeScript",
        "ui": "Ant Design / Shadcn UI",
        "state": "Zustand / Redux Toolkit",
        "editor": "Monaco Editor (代码编辑)",
        "flow": "React Flow (可视化编排)"
    },
    "backend": {
        "framework": "FastAPI",
        "database": "PostgreSQL",
        "cache": "Redis",
        "queue": "Celery + RabbitMQ",
        "storage": "MinIO / S3 (配置存储)"
    },
    "auth": {
        "system": "Auth0 / Keycloak",
        "strategy": "JWT + OAuth 2.0"
    }
}
```

### 3.3 界面设计

```
┌─────────────────────────────────────────────────────────────┐
│  Logo  |  Crews  |  Templates  |  Strategies  |  Team  |  👤 │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  📁 我的 Crews                                  [+ 新建]    │
│  ├─ 📊 数据分析 Crew                          [编辑] [运行] │
│  ├─ 🎯 策略规划 Crew                          [编辑] [运行] │
│  ├─ ⚡ 批量执行 Crew                          [编辑] [运行] │
│  └─ 🔍 合规审查 Crew                          [编辑] [运行] │
│                                                              │
│  📚 模板市场                                                 │
│  ├─ ⭐ 热门模板                                              │
│  │   ├─ 亚马逊运营助手                      [使用] [预览]   │
│  │   ├─ eBay 数据分析                       [使用] [预览]   │
│  │   └─ 多平台策略生成                      [使用] [预览]   │
│  └─ 🏢 团队模板                                              │
│      ├─ 定价策略模板                         [使用] [编辑]   │
│      └─ 库存优化模板                         [使用] [编辑]   │
│                                                              │
│  📈 执行历史                                                 │
│  ┌────────────────────────────────────────────────────┐    │
│  │ 时间          │ Crew        │ 状态    │ 耗时 │ 成本 │    │
│  │ 10:30        │ 数据分析     │ ✅ 成功 │ 2.3s │ $0.05│    │
│  │ 10:25        │ 策略规划     │ ✅ 成功 │ 5.1s │ $0.12│    │
│  │ 10:20        │ 批量执行     │ ❌ 失败 │ 1.2s │ $0.02│    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

## 4. 桌面客户端设计

### 4.1 核心功能

```
┌─────────────────────────────────────────────────────────────┐
│                    桌面客户端                                │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  1. 对话式交互 (类似 Claude Code)                  │    │
│  │  • 自然语言对话                                     │    │
│  │  • 上下文理解                                       │    │
│  │  • 智能建议                                         │    │
│  │  • 多轮对话                                         │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  2. 本地 Crew 执行引擎                             │    │
│  │  • Crew AI 1.3.0 运行时                            │    │
│  │  • 本地 LLM 支持 (Ollama)                          │    │
│  │  • 云端 LLM 支持 (OpenAI/Claude)                   │    │
│  │  • 执行状态实时显示                                │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  3. MCP 集成                                        │    │
│  │  • MCP 服务管理                                     │    │
│  │  • 工具发现与注册                                   │    │
│  │  • 工具调用监控                                     │    │
│  │  • 错误处理                                         │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  4. 配置同步                                        │    │
│  │  • 从 Web 端拉取配置                               │    │
│  │  • 本地配置管理                                     │    │
│  │  • 离线模式                                         │    │
│  │  • 冲突解决                                         │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 技术栈

```python
# 桌面客户端技术栈
{
    "framework": "Electron / Tauri",
    "frontend": {
        "framework": "React 18 + TypeScript",
        "ui": "Tailwind CSS + Shadcn UI",
        "chat": "自定义聊天组件"
    },
    "backend": {
        "runtime": "Python (Crew AI)",
        "ipc": "Electron IPC / Tauri Commands",
        "mcp": "MCP Python SDK"
    },
    "local_llm": {
        "provider": "Ollama",
        "models": ["llama3.1:70b", "mistral:latest"]
    },
    "storage": {
        "config": "SQLite",
        "cache": "LevelDB",
        "files": "本地文件系统"
    }
}
```

### 4.3 界面设计

```
┌─────────────────────────────────────────────────────────────┐
│  🏠 主页  |  💬 对话  |  🤖 Crews  |  🔧 工具  |  ⚙️ 设置   │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  💬 运营助手对话                                [清空] [设置]│
│  ┌────────────────────────────────────────────────────┐    │
│  │                                                     │    │
│  │  👤 你: 帮我分析一下店铺 A 最近的销售情况           │    │
│  │                                                     │    │
│  │  🤖 助手: 好的，我来帮你分析店铺 A 的数据。         │    │
│  │                                                     │    │
│  │  [正在执行] 数据收集 Crew...                        │    │
│  │  ✅ 已收集 30 天销售数据                            │    │
│  │  ✅ 已收集库存数据                                  │    │
│  │                                                     │    │
│  │  [正在执行] 数据分析 Crew...                        │    │
│  │  ✅ 趋势分析完成                                    │    │
│  │                                                     │    │
│  │  📊 分析结果：                                      │    │
│  │  • 销量增长 15% (1000 → 1150件)                    │    │
│  │  • 转化率下降 2% (3.5% → 3.43%)                    │    │
│  │  • 客单价上涨 8% ($45 → $48.6)                     │    │
│  │                                                     │    │
│  │  💡 建议：                                          │    │
│  │  1. 优化商品详情页提升转化率                       │    │
│  │  2. 考虑推出组合促销                               │    │
│  │                                                     │    │
│  │  需要我生成详细的优化方案吗？                       │    │
│  │                                                     │    │
│  │  👤 你: 是的，生成方案                              │    │
│  │                                                     │    │
│  │  🤖 助手: [正在执行] 策略规划 Crew...               │    │
│  │                                                     │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  [输入消息...                                        ] [发送]│
│                                                              │
│  💡 快捷操作:                                                │
│  [分析店铺] [生成策略] [批量操作] [查看报告]               │
└─────────────────────────────────────────────────────────────┘
```

## 5. MCP 集成设计

### 5.1 什么是 MCP？

**MCP (Model Context Protocol)** 是一个开放协议，用于连接 AI 应用与外部工具/数据源。

```
┌─────────────────────────────────────────────────────────────┐
│                    MCP 架构                                  │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  AI Application (Crew AI Client)                   │    │
│  │  • 发现可用工具                                     │    │
│  │  • 调用工具                                         │    │
│  │  • 接收结果                                         │    │
│  └──────────────────┬─────────────────────────────────┘    │
│                     │ MCP Protocol                          │
│                     ↓                                        │
│  ┌────────────────────────────────────────────────────┐    │
│  │  MCP Server                                         │    │
│  │  • 注册工具                                         │    │
│  │  • 处理请求                                         │    │
│  │  • 返回结果                                         │    │
│  └──────────────────┬─────────────────────────────────┘    │
│                     │                                        │
│                     ↓                                        │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Local Tools / Resources                            │    │
│  │  • 文件系统                                         │    │
│  │  • 数据库                                           │    │
│  │  • 内部 API                                         │    │
│  │  • 本地脚本                                         │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 MCP 工具示例

```python
# MCP Server 实现示例
from mcp import Server, Tool

class ShopDataMCPServer(Server):
    """店铺数据 MCP 服务器"""
    
    def __init__(self):
        super().__init__(name="shop-data-server")
        
        # 注册工具
        self.register_tool(QueryLocalDatabaseTool())
        self.register_tool(ReadExcelFileTool())
        self.register_tool(CallInternalAPITool())
        self.register_tool(ExecuteLocalScriptTool())

class QueryLocalDatabaseTool(Tool):
    """查询本地数据库工具"""
    
    name = "query_local_database"
    description = "查询本地 MySQL/PostgreSQL 数据库"
    
    parameters = {
        "query": {"type": "string", "description": "SQL 查询语句"},
        "database": {"type": "string", "description": "数据库名称"}
    }
    
    def execute(self, query: str, database: str):
        """执行查询"""
        # 连接本地数据库
        conn = self._get_connection(database)
        
        # 执行查询
        result = conn.execute(query)
        
        return {
            "success": True,
            "rows": result.fetchall(),
            "columns": result.keys()
        }

class ReadExcelFileTool(Tool):
    """读取 Excel 文件工具"""
    
    name = "read_excel_file"
    description = "读取本地 Excel 文件"
    
    parameters = {
        "file_path": {"type": "string", "description": "文件路径"},
        "sheet_name": {"type": "string", "description": "工作表名称"}
    }
    
    def execute(self, file_path: str, sheet_name: str = None):
        """读取 Excel"""
        import pandas as pd
        
        # 读取文件
        df = pd.read_excel(file_path, sheet_name=sheet_name)
        
        return {
            "success": True,
            "data": df.to_dict('records'),
            "columns": df.columns.tolist(),
            "rows": len(df)
        }

class CallInternalAPITool(Tool):
    """调用内部 API 工具"""
    
    name = "call_internal_api"
    description = "调用公司内部 API"
    
    parameters = {
        "endpoint": {"type": "string", "description": "API 端点"},
        "method": {"type": "string", "description": "HTTP 方法"},
        "data": {"type": "object", "description": "请求数据"}
    }
    
    def execute(self, endpoint: str, method: str = "GET", data: dict = None):
        """调用 API"""
        import requests
        
        # 从配置读取 API 基础 URL 和认证信息
        base_url = self.config.get("internal_api_base_url")
        auth_token = self.config.get("internal_api_token")
        
        # 发起请求
        response = requests.request(
            method=method,
            url=f"{base_url}{endpoint}",
            json=data,
            headers={"Authorization": f"Bearer {auth_token}"}
        )
        
        return {
            "success": response.ok,
            "status_code": response.status_code,
            "data": response.json()
        }
```

### 5.3 在 Crew AI 中使用 MCP 工具

```python
from crewai import Agent, Task, Crew
from crewai_tools import MCPTool

# 创建 MCP 工具
query_db_tool = MCPTool(
    server="shop-data-server",
    tool="query_local_database"
)

read_excel_tool = MCPTool(
    server="shop-data-server",
    tool="read_excel_file"
)

call_api_tool = MCPTool(
    server="shop-data-server",
    tool="call_internal_api"
)

# 创建 Agent，使用 MCP 工具
data_agent = Agent(
    role="数据分析师",
    goal="从本地数据源收集和分析数据",
    backstory="你可以访问本地数据库、Excel 文件和内部 API...",
    tools=[
        query_db_tool,
        read_excel_tool,
        call_api_tool
    ],
    verbose=True
)

# 创建任务
analysis_task = Task(
    description="""
    分析店铺 A 的销售数据：
    
    1. 从本地数据库查询最近30天的销售记录
    2. 读取库存 Excel 文件
    3. 调用内部 API 获取订单状态
    4. 综合分析并生成报告
    """,
    agent=data_agent,
    expected_output="详细的数据分析报告"
)

# 执行
crew = Crew(agents=[data_agent], tasks=[analysis_task])
result = crew.kickoff()
```

## 6. 数据同步机制

### 6.1 同步策略

```
┌─────────────────────────────────────────────────────────────┐
│                    同步策略                                  │
│                                                              │
│  1. 配置同步 (Web → Client)                                 │
│     • Crew 配置                                              │
│     • Agent 模板                                             │
│     • Task 定义                                              │
│     • 策略模板                                               │
│     同步时机: 启动时 / 手动触发 / 定时同步                   │
│                                                              │
│  2. 执行日志上传 (Client → Web)                             │
│     • 执行记录                                               │
│     • 性能指标                                               │
│     • 错误日志                                               │
│     • 成本统计                                               │
│     上传时机: 执行完成后 / 批量上传                          │
│                                                              │
│  3. 冲突解决                                                 │
│     • 本地优先: 本地修改覆盖云端                            │
│     • 云端优先: 云端配置覆盖本地                            │
│     • 手动合并: 用户选择保留哪个版本                        │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 实现示例

```python
class SyncManager:
    """同步管理器"""
    
    def __init__(self, web_api_url: str, auth_token: str):
        self.web_api_url = web_api_url
        self.auth_token = auth_token
        self.local_db = LocalDatabase()
    
    async def sync_from_web(self):
        """从 Web 端同步配置"""
        # 1. 获取云端配置列表
        cloud_configs = await self._fetch_cloud_configs()
        
        # 2. 获取本地配置列表
        local_configs = self.local_db.get_all_configs()
        
        # 3. 比较版本
        for cloud_config in cloud_configs:
            local_config = local_configs.get(cloud_config["id"])
            
            if not local_config:
                # 新配置，直接下载
                await self._download_config(cloud_config)
            elif cloud_config["version"] > local_config["version"]:
                # 云端版本更新，检查冲突
                if local_config["modified"]:
                    # 有本地修改，需要解决冲突
                    await self._resolve_conflict(cloud_config, local_config)
                else:
                    # 无本地修改，直接更新
                    await self._download_config(cloud_config)
    
    async def sync_to_web(self, execution_log: dict):
        """上传执行日志到 Web 端"""
        try:
            response = await self._upload_log(execution_log)
            return response
        except Exception as e:
            # 上传失败，保存到本地队列
            self.local_db.add_to_upload_queue(execution_log)
            logging.error(f"Failed to upload log: {e}")
    
    async def _fetch_cloud_configs(self):
        """获取云端配置"""
        response = await httpx.get(
            f"{self.web_api_url}/api/configs",
            headers={"Authorization": f"Bearer {self.auth_token}"}
        )
        return response.json()
    
    async def _resolve_conflict(self, cloud_config, local_config):
        """解决冲突"""
        # 显示冲突解决对话框
        choice = await self._show_conflict_dialog(cloud_config, local_config)
        
        if choice == "keep_local":
            # 保留本地，上传到云端
            await self._upload_config(local_config)
        elif choice == "use_cloud":
            # 使用云端，覆盖本地
            await self._download_config(cloud_config)
        else:
            # 手动合并
            merged = await self._manual_merge(cloud_config, local_config)
            await self._save_merged_config(merged)
```

## 7. 离线模式

### 7.1 离线能力

```
┌─────────────────────────────────────────────────────────────┐
│                    离线模式                                  │
│                                                              │
│  ✅ 完全离线可用                                             │
│     • 使用本地缓存的配置                                     │
│     • 使用本地 LLM (Ollama)                                 │
│     • 调用本地 MCP 工具                                     │
│     • 本地数据库存储                                         │
│                                                              │
│  ⚠️ 受限功能                                                │
│     • 无法同步最新配置                                       │
│     • 无法上传执行日志                                       │
│     • 无法使用云端 LLM                                      │
│     • 无法访问云端模板库                                     │
│                                                              │
│  🔄 恢复在线后                                               │
│     • 自动同步配置                                           │
│     • 批量上传离线期间的日志                                 │
│     • 解决可能的冲突                                         │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 实现示例

```python
class OfflineMode:
    """离线模式管理"""
    
    def __init__(self):
        self.is_online = self._check_connection()
        self.offline_queue = []
    
    def execute_crew(self, crew_config: dict):
        """执行 Crew（支持离线）"""
        if self.is_online:
            # 在线模式：使用云端 LLM
            return self._execute_online(crew_config)
        else:
            # 离线模式：使用本地 LLM
            return self._execute_offline(crew_config)
    
    def _execute_offline(self, crew_config: dict):
        """离线执行"""
        # 1. 使用本地 LLM
        from langchain_community.llms import Ollama
        
        local_llm = Ollama(model="llama3.1:70b")
        
        # 2. 创建 Agent（使用本地 LLM）
        agent = Agent(
            role=crew_config["agent"]["role"],
            goal=crew_config["agent"]["goal"],
            llm=local_llm,  # 本地 LLM
            tools=self._get_local_tools(),  # 本地工具
            verbose=True
        )
        
        # 3. 执行
        result = crew.kickoff()
        
        # 4. 保存到离线队列（等待上传）
        self.offline_queue.append({
            "crew_id": crew_config["id"],
            "result": result,
            "timestamp": datetime.now()
        })
        
        return result
    
    async def sync_when_online(self):
        """恢复在线后同步"""
        if not self.is_online:
            return
        
        # 上传离线期间的执行日志
        for log in self.offline_queue:
            try:
                await self._upload_log(log)
                self.offline_queue.remove(log)
            except Exception as e:
                logging.error(f"Failed to upload offline log: {e}")
```

## 8. 部署方案

### 8.1 Web 端部署

```yaml
# docker-compose.yml
version: '3.8'

services:
  # Web 前端
  web-frontend:
    build: ./web-frontend
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_API_URL=http://api:8000

  # API 后端
  api:
    build: ./api-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/crewai_manager
      - REDIS_URL=redis://redis:6379
    depends_on:
      - db
      - redis

  # PostgreSQL
  db:
    image: postgres:15
    volumes:
      - postgres-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=password

  # Redis
  redis:
    image: redis:7
    volumes:
      - redis-data:/data

volumes:
  postgres-data:
  redis-data:
```

### 8.2 客户端打包

```json
// package.json
{
  "name": "crewai-desktop-client",
  "version": "1.0.0",
  "scripts": {
    "dev": "electron .",
    "build:win": "electron-builder --win",
    "build:mac": "electron-builder --mac",
    "build:linux": "electron-builder --linux"
  },
  "build": {
    "appId": "com.company.crewai-client",
    "productName": "CrewAI 运营助手",
    "files": [
      "dist/**/*",
      "python/**/*",
      "node_modules/**/*"
    ],
    "extraResources": [
      {
        "from": "python/",
        "to": "python/",
        "filter": ["**/*"]
      }
    ],
    "win": {
      "target": ["nsis"],
      "icon": "assets/icon.ico"
    },
    "mac": {
      "target": ["dmg"],
      "icon": "assets/icon.icns"
    },
    "linux": {
      "target": ["AppImage"],
      "icon": "assets/icon.png"
    }
  }
}
```

## 9. 总结

### 9.1 架构优势

| 特性 | 优势 |
|------|------|
| **集中管理** | ✅ Web 端统一管理配置、模板、策略 |
| **本地能力** | ✅ 客户端完整的 MCP 和本地工具支持 |
| **团队协作** | ✅ Web 端支持多用户、权限、审批 |
| **离线工作** | ✅ 客户端支持完全离线模式 |
| **数据安全** | ✅ 敏感数据保留在本地 |
| **灵活部署** | ✅ 支持云端部署和私有部署 |

### 9.2 实施路线图

**Phase 1: MVP (4-6 周)**
- [ ] Web 端基础功能（Crew 配置管理）
- [ ] 客户端基础框架（Electron + React）
- [ ] 本地 Crew 执行引擎
- [ ] 基础 MCP 集成（文件系统、数据库）

**Phase 2: 核心功能 (6-8 周)**
- [ ] Web 端模板库
- [ ] 客户端对话式交互
- [ ] 完整 MCP 工具集
- [ ] 配置同步机制

**Phase 3: 高级功能 (4-6 周)**
- [ ] 团队协作功能
- [ ] 执行监控与分析
- [ ] 离线模式
- [ ] 性能优化

**Phase 4: 生产就绪 (4-6 周)**
- [ ] 安全加固
- [ ] 完整测试
- [ ] 文档完善
- [ ] 部署上线

### 9.3 关键决策

✅ **推荐的架构**：
- Web 端：React + FastAPI
- 客户端：Electron + Python (Crew AI)
- MCP：Python MCP SDK
- 同步：RESTful API + WebSocket

✅ **核心价值**：
- 集中管理 + 本地能力的完美结合
- 类似 Claude Code 的体验，但专注运营场景
- 团队协作 + 个人效率的平衡

---

**文档版本**: v1.0  
**创建日期**: 2025-11-06  
**适用场景**: 跨境电商智能运营系统

